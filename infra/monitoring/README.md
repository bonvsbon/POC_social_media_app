# Monitoring Placeholders

- Metrics: request latency, error rate, queue lag, moderation auto-action ratio.
- Logs: structured JSON logs with request and user correlation IDs.
- Tracing: OpenTelemetry for API and worker pipelines.
- Alerts: queue lag, 5xx burst, payout failure rate, moderation false-positive spikes.
